import { HOME, REGISTRO, USUARIOS } from "@features/constants/pages";

const nextForm = 
`*=>${HOME},*=>${REGISTRO},*=>${USUARIOS}`

var previousForm = `=>home`

export {nextForm, previousForm}